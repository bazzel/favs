{{#paper-toolbar class="flex-none md-whiteframe-1dp" as |toolbar|}}
  {{#toolbar.tools}}
    {{#paper-sidenav-toggle as |toggleAction|}}
      {{#paper-button onClick=(action toggleAction) iconButton=true}}
        {{paper-icon "menu"}}
      {{/paper-button}}
    {{/paper-sidenav-toggle}}
    <h2>
      {{this.pageTitle}}
    </h2>
  {{/toolbar.tools}}
{{/paper-toolbar}}
